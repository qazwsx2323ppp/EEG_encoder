# requirements.txt

# --- Core Deep Learning & Numerics ---
# 安装与服务器 CUDA 版本匹配的 PyTorch
# 访问 https://pytorch.org/get-started/locally/ 获取精确命令
# 例如: torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# (此处仅列出包名，具体安装命令需根据服务器环境确定)
torch
torchvision
torchaudio
numpy

# --- EEG Processing ---
# 根据之前的讨论，使用 0.7 版本以兼容 clip_models.py 中的导入
braindecode==0.7 
mne # braindecode 通常依赖 MNE

# --- Configuration & Logging ---
hydra-core
omegaconf
wandb 
tqdm # 用于显示进度条

# --- Utilities & Metrics (可能需要) ---
scikit-learn # 用于可能的评估指标或 braindecode 依赖

# --- Pydantic (Hydra/WandB 依赖, 用于处理警告) ---
# 可以指定一个较新版本尝试解决警告, 但需确保兼容性
# pydantic >= 2.0 
# 或者暂时不指定版本，让 pip 自动选择

# --- (可选) 加速库 ---
# accelerate # 如果服务器有多GPU或需要混合精度，可以考虑